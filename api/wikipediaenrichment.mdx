---
title: "wikipediaEnrichment"
description: "Service: Wikipedia Enrichment Service — runs server-side (Node.js / wizard-server). Handles all Wikipedia API calls so the Electron renderer doesn't need to. Returns lightweight metadata (URLs as strings, never image binaries). IMPORTANT: This file is imported by wizard-server.js. NEVER use console.log() — use console.error() for logging (MCP stdio rule)."
---

# wikipediaEnrichment

Wikipedia Enrichment Service — runs server-side (Node.js / wizard-server). Handles all Wikipedia API calls so the Electron renderer doesn't need to. Returns lightweight metadata (URLs as strings, never image binaries). IMPORTANT: This file is imported by wizard-server.js. NEVER use console.log() — use console.error() for logging (MCP stdio rule).

## Location
`src/wizard/services/wikipediaEnrichment.js`

## Functions


### normalizeLabel

```javascript
function normalizeLabel(
```

No description available.


### levenshteinDistance

```javascript
function levenshteinDistance(
```

No description available.


### calculateTextSimilarity

```javascript
function calculateTextSimilarity(
```

No description available.


### calculateWikipediaConfidence

```javascript
function calculateWikipediaConfidence(
```

No description available.


### getWikidataIdFromWikipedia

```javascript
function getWikidataIdFromWikipedia(
```

No description available.


### fetchWikipediaSummary

```javascript
function fetchWikipediaSummary(
```

No description available.


### resolveWikipediaMatch

```javascript
function resolveWikipediaMatch(
```

No description available.


### enrichBatch

```javascript
function enrichBatch(
```

No description available.


### worker

```javascript
function worker(
```

No description available.


### enrichSingle

```javascript
function enrichSingle(
```

No description available.


---

*Auto-generated from source code*
