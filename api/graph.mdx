---
title: "Graph"
description: "Class: Represents the graph structure containing nodes and edges. Stores nodes and edges by their string IDs. Extends the base Entry class. Equivalent to Graph.java."
---

# Graph <Badge>extends Entry</Badge>

Represents the graph structure containing nodes and edges. Stores nodes and edges by their string IDs. Extends the base Entry class. Equivalent to Graph.java.

## Location
`src/core/Graph.js`

## Methods

- `isDirected()`
- `getNodes()`
- `getNodeById()`
- `addNode()`
- `updateNode()`
- `removeNode()`
- `if()`
- `getEdges()`
- `getEdgeById()`
- `addEdge()`
- `if()`
- `if()`
- `updateEdge()`
- `removeEdge()`
- `if()`
- `if()`
- `if()`
- `clone()`
- `deepClone()`
- `compose()`

## Properties

- `nodes`
- `edges`
- `directed`

---

*Auto-generated from source code*
