---
title: "orbitLocalIndex"
description: "Service: Ingests catalog entries and optionally upserts protected prototypes into the store. Each entry shape: \{ uri: 'http://example.org/entity', label: 'Example', description?: '...', types?: ['Q5'], source?: 'orbit-local', related?: [\{ uri, label, predicate?, source?, types? \}] \}"
---

# orbitLocalIndex

Ingests catalog entries and optionally upserts protected prototypes into the store. Each entry shape: \{ uri: 'http://example.org/entity', label: 'Example', description?: '...', types?: ['Q5'], source?: 'orbit-local', related?: [\{ uri, label, predicate?, source?, types? \}] \}

## Location
`src/services/orbitLocalIndex.js`

## Methods

No methods detected.

---

*Auto-generated from source code*
