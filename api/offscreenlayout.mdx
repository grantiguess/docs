---
title: "offscreenLayout"
description: "Service: Compute and persist a force-directed layout for any graph, active or not. Uses estimated node dimensions (no DOM access required) and a fixed 2000×2000 virtual canvas. Positions are written directly to the store."
---

# offscreenLayout

Compute and persist a force-directed layout for any graph, active or not. Uses estimated node dimensions (no DOM access required) and a fixed 2000×2000 virtual canvas. Positions are written directly to the store.

## Location
`src/services/offscreenLayout.js`

## Methods

No methods detected.

---

*Auto-generated from source code*
