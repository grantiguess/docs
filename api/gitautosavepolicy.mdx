---
title: "GitAutosavePolicy"
description: "Service: Git Autosave Policy - Implementation of intelligent auto-commit policy Features: - Debounce edits: commit after 10s idle OR 90s max - Batch changes: 1 commit per burst, never per keystroke - Cap: ≤60 commits/hr/repo/user - Serialize writes; ≥1s gap between mutating API calls - Backoff on 403/Retry-After; exponential retry - File size: \<10MiB ideal, \<50MiB soft cap, never ≥100MB - Repo size monitoring with LFS recommendations - Periodic squash: condense autosave commits daily/weekly - Shallow/partial clones recommended - CI: filter autosave commits to avoid workflow storms"
---

# GitAutosavePolicy

Git Autosave Policy - Implementation of intelligent auto-commit policy Features: - Debounce edits: commit after 10s idle OR 90s max - Batch changes: 1 commit per burst, never per keystroke - Cap: ≤60 commits/hr/repo/user - Serialize writes; ≥1s gap between mutating API calls - Backoff on 403/Retry-After; exponential retry - File size: \<10MiB ideal, \<50MiB soft cap, never ≥100MB - Repo size monitoring with LFS recommendations - Periodic squash: condense autosave commits daily/weekly - Shallow/partial clones recommended - CI: filter autosave commits to avoid workflow storms

## Location
`src/services/GitAutosavePolicy.js`

## Methods


### initialize

Initialize with dependencies

---

### onStatusChange

Status notification system

---

### onEditActivity

Handle edit activity - core debouncing logic

---

### getEffectiveTimeouts

Get effective debounce timeouts based on the Git engine's source-of-truth mode. When local is primary, Git is a backup and uses a tighter, simpler cadence (commits within ~10s of edits, no 90s burst tolerance). When Git is primary, the original 10s-idle / 90s-max debounce applies.

---

### clearPending

Drops any queued batch and cancels pending commit timers. Called on universe switch: the policy is a singleton whose engine pointer gets hot-swapped, so a timer armed for the OLD universe must never fire a commit through the NEW universe's engine. (The engine also refuses wrong-slug state as a hard guard; this prevents the attempt.)

---

### executeBatchCommit

Execute batch commit with policy enforcement

---

### checkRateLimit

Check rate limiting (≤60 commits/hour)

---

### checkApiGap

Check API gap requirement (≥1s between calls)

---

### checkFileSize

Check file size constraints

---

### handleCommitError

Handle commit errors with exponential backoff

---

### extractRetryAfter

Extract retry-after header from error

---

### scheduleRetryAfterRateLimit

Schedule retry after rate limit window

---

### getCurrentState

Get current state for commit

---

### performCommit

Perform actual commit

---

### cleanCommitHistory

Clean old commit history

---

### formatSize

Format file size for display

---

### forceCommit

Force immediate commit (manual save)

---

### getStatus

Get current policy status

---

### setEnabled

Enable/disable the policy

---

### destroy

Cleanup


---

*Auto-generated from source code*
