---
title: "Edge"
description: "Class: Represents an edge connecting two nodes in the graph. Extends the base Entry class. Stores references to nodes via their string IDs. Equivalent to Edge.java."
---

# Edge <Badge>extends Entry</Badge>

Represents an edge connecting two nodes in the graph. Extends the base Entry class. Stores references to nodes via their string IDs. Equivalent to Edge.java.

## Location
`src/core/Edge.js`

## Methods

- `if()`
- `if()`
- `if()`
- `if()`
- `getSourceId()`
- `getDestinationId()`
- `getDefinitionNodeIds()`
- `addDefinitionNodeId()`
- `if()`
- `clone()`

## Properties

- `sourceId`
- `destinationId`
- `definitionNodeIds`
- `directionality`

---

*Auto-generated from source code*
