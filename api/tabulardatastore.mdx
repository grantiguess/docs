---
title: "tabularDataStore"
description: "Service: Temporary in-memory storage for parsed tabular data. File attachments are parsed client-side and stored here so that wizard tools (via config passthrough) can access the full dataset. Designed for both in-app wizard and future UI import features."
---

# tabularDataStore

Temporary in-memory storage for parsed tabular data. File attachments are parsed client-side and stored here so that wizard tools (via config passthrough) can access the full dataset. Designed for both in-app wizard and future UI import features.

## Location
`src/services/tabularDataStore.js`

## Functions


### storeTabularData

```javascript
export function storeTabularData(
```

No description available.


### getTabularData

```javascript
export function getTabularData(
```

No description available.


### getTabularDataByIndex

```javascript
export function getTabularDataByIndex(
```

No description available.


### getFirstTabularData

```javascript
export function getFirstTabularData(
```

No description available.


### getAllTabularData

```javascript
export function getAllTabularData(
```

No description available.


### clearTabularData

```javascript
export function clearTabularData(
```

No description available.


### pruneStaleData

```javascript
export function pruneStaleData(
```

No description available.


### getStoreSize

```javascript
export function getStoreSize(
```

No description available.


---

*Auto-generated from source code*
