---
title: "UpdateToast"
description: "React component: UpdateToast — bottom-right notification for Electron auto-update lifecycle. Shows as soon as a release is detected (not just when fully downloaded), so a stalled download doesn't silently hide the fact that an update exists."
---

# UpdateToast

UpdateToast — bottom-right notification for Electron auto-update lifecycle. Shows as soon as a release is detected (not just when fully downloaded), so a stalled download doesn't silently hide the fact that an update exists.

## Location
`src/components/UpdateToast.jsx`

## Props

This component does not accept props.

## Hooks Used

- `useState`
- `useEffect`
- `useRef`
- `used`

## Dependencies

- `lucide-react`
- `./shared/PanelIconButton.jsx`
- `./UpdateDiagnosticsCard.jsx`

---

*Auto-generated from source code*
