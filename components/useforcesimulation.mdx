---
title: "useForceSimulation"
description: "React component: Custom hook encapsulating the force-directed simulation engine. Handles all physics computation, animation loop, parameter management, and auto-start/auto-stop behavior. No UI concerns."
---

# useForceSimulation

Custom hook encapsulating the force-directed simulation engine. Handles all physics computation, animation loop, parameter management, and auto-start/auto-stop behavior. No UI concerns.

## Location
`src/hooks/useForceSimulation.js`

## Props

This component does not accept props.

## Hooks Used

- `useState`
- `useRef`
- `useEffect`
- `useCallback`
- `useForceSimulation`
- `uses`

## Dependencies

- `react`
- `../services/graphLayoutService.js`

---

*Auto-generated from source code*
