---
title: "findDuplicateNode"
description: "React component: AI-Driven Duplicate Detection Uses LLM to intelligently detect if nodes are duplicates or near-duplicates, even when names differ slightly (fuzzy matching with semantic understanding)."
---

# findDuplicateNode

AI-Driven Duplicate Detection Uses LLM to intelligently detect if nodes are duplicates or near-duplicates, even when names differ slightly (fuzzy matching with semantic understanding).

## Location
`src/services/aiDuplicateDetector.js`

## Props

This component does not accept props.

## Hooks Used

- `useful`

## Dependencies

No imports detected.

---

*Auto-generated from source code*
