---
title: "GitNativeFederationPureUI"
description: "React component: Example: GitNativeFederation as Pure UI Component This shows how GitNativeFederation SHOULD work - as a pure UI component that only displays data and calls backend services. NO backend logic, NO engine creation, NO authentication handling. Just UI and calls to universeBackend."
---

# GitNativeFederationPureUI

Example: GitNativeFederation as Pure UI Component This shows how GitNativeFederation SHOULD work - as a pure UI component that only displays data and calls backend services. NO backend logic, NO engine creation, NO authentication handling. Just UI and calls to universeBackend.

## Location
`src/examples/GitNativeFederationPureUI.jsx`

## Props

This component does not accept props.

## Hooks Used

- `useState`
- `useEffect`
- `user`

## Dependencies

- `lucide-react`
- `../services/universeBackend.js`

---

*Auto-generated from source code*
