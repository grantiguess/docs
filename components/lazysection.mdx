---
title: "LazySection"
description: "React component: Culls offscreen sections by only mounting children when the section is within `rootMargin` of the scroll viewport. When not visible, renders a placeholder div preserving the estimated height so scroll position stays stable."
---

# LazySection

Culls offscreen sections by only mounting children when the section is within `rootMargin` of the scroll viewport. When not visible, renders a placeholder div preserving the estimated height so scroll position stays stable.

## Location
`src/components/panel/LazySection.jsx`

## Props

This component does not accept props.

## Hooks Used

- `useRef`
- `useState`
- `useEffect`

## Dependencies

No imports detected.

---

*Auto-generated from source code*
