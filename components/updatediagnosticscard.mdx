---
title: "UpdateDiagnosticsCard"
description: "React component: UpdateDiagnosticsCard — popover above UpdateToast (or standalone bottom-right) surfacing the full updater state so wedged installs can be diagnosed and recovered without spelunking the filesystem."
---

# UpdateDiagnosticsCard

UpdateDiagnosticsCard — popover above UpdateToast (or standalone bottom-right) surfacing the full updater state so wedged installs can be diagnosed and recovered without spelunking the filesystem.

## Location
`src/components/UpdateDiagnosticsCard.jsx`

## Props

This component does not accept props.

## Hooks Used

- `useEffect`
- `useState`
- `useCallback`

## Dependencies

- `lucide-react`
- `./shared/PanelIconButton.jsx`

---

*Auto-generated from source code*
