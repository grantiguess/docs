---
title: "WizardActionChip"
description: "React component: Compact, right-aligned card representing a programmatic Ask The Wizard action (e.g., refine a connection or define a node's components). Visually identical to the AI's tool-call cards on the wizard side — same CSS classes and layout — just rendered on the user side. No expand/dropdown; the rich prompt is sent to the model but never shown to the user."
---

# WizardActionChip

Compact, right-aligned card representing a programmatic Ask The Wizard action (e.g., refine a connection or define a node's components). Visually identical to the AI's tool-call cards on the wizard side — same CSS classes and layout — just rendered on the user side. No expand/dropdown; the rich prompt is sent to the model but never shown to the user.

## Location
`src/components/wizard/WizardActionChip.jsx`

## Props

This component does not accept props.

## Hooks Used

- `user`

## Dependencies

- `react`
- `lucide-react`

---

*Auto-generated from source code*
