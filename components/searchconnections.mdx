---
title: "searchConnections"
description: "React component: searchConnections - Find connections/edges by type, node names, or general query Searches through all edges in the active graph, matching against connection types, source/target node names, and descriptions. Returns results ranked by relevance."
---

# searchConnections

searchConnections - Find connections/edges by type, node names, or general query Searches through all edges in the active graph, matching against connection types, source/target node names, and descriptions. Returns results ranked by relevance.

## Location
`src/wizard/tools/searchConnections.js`

## Props

This component does not accept props.

## Hooks Used

- `used`

## Dependencies

- `./resolveGraphId.js`

---

*Auto-generated from source code*
