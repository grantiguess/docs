---
title: "evictOldest"
description: "React component: Text measurement service powered by @chenglou/pretext. Replaces DOM-based hidden-element measurement (offsetWidth/offsetHeight) and canvas measureText() calls with Pretext's DOM-free layout engine, eliminating synchronous layout reflows."
---

# evictOldest

Text measurement service powered by @chenglou/pretext. Replaces DOM-based hidden-element measurement (offsetWidth/offsetHeight) and canvas measureText() calls with Pretext's DOM-free layout engine, eliminating synchronous layout reflows.

## Location
`src/services/textMeasurement.js`

## Props

This component does not accept props.

## Hooks Used

No React hooks detected.

## Dependencies

- `@chenglou/pretext`

---

*Auto-generated from source code*
